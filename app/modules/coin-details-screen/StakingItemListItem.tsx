import { StyleSheet, View } from "react-native";

const StakingItemListContainer = () => {
  return(
    <View>

    </View>
  );
}

export default StakingItemListContainer;

const styles = StyleSheet.create({
  container: {
    flex: 1
  }
});